[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
log_cli = true
log_cli_level = INFO
markers =
    unit: Unit tests (fast, no I/O)
    functional: Functional tests (mocked I/O)
    integration: Integration tests (real services)
